# Зависимости скрипта superset/apply_review_fixes.py.
#
# Намеренно НЕ включает requirements.txt: скрипт не трогает данные, ему
# не нужны ни pandas, ни pyspark — только HTTP к Superset API и growth.py
# из стандартной библиотеки. Тянуть сюда весь runtime пайплайна значило
# бы ставить полгигабайта ради одного клиента.
requests>=2.31
