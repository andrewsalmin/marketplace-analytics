"""Клиент Superset: применение правок дашборда через REST API.

Пакет, а не просто каталог со скриптом, ради импорта по имени:

    from superset.apply_review_fixes import Runner

Пока каталог был обычным, тесты грузили файл по физическому пути через
importlib.util.spec_from_file_location — тот же долг, что и правка
sys.path, только другим механизмом. Заодно такой импорт обходит
обычную машинерию, и модуль, убивавший процесс при загрузке, слишком
долго выглядел рабочим.
"""
